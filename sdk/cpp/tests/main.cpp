/**
 * the0 C++ SDK Test Suite
 *
 * Main entry point for doctest. Individual tests are in separate files:
 * - test_parse.cpp: Tests for parse() and parse_raw() functions
 * - test_result.cpp: Tests for success(), error(), and result() functions
 * - test_metric.cpp: Tests for metric() and log() functions
 */

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
