{
  "name": "price-alerts-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Realtime price monitoring bot with alerts",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --platform=node --target=node20 --outfile=main.js --external:pino",
    "build:sdk": "esbuild main_sdk.ts --bundle --platform=node --target=node20 --outfile=main_sdk.js"
  },
  "dependencies": {
    "pino": "^9.0.0",
    "@alexanderwanyoike/the0-node": "file:../../sdk/nodejs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
